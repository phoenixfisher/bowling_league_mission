{
  "name": "fullstack-test",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "dotnet run --project backend/backend.csproj",
    "build": "npm --prefix frontend run build && dotnet build test.sln",
    "lint": "npm --prefix frontend run lint"
  }
}
